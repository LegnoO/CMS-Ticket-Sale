/** @format */

import React from "react";
import classNames from "classnames/bind";
import styles from "./TicketFilter.module.scss";
const cx = classNames.bind(styles);

const TicketFilter = () => {
  return <div classNames={cx("wrapper")}></div>;
};

export default TicketFilter;
